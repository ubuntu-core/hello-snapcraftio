#/bin/sh

# 1.x versions are in the stable channel and prints "Hello world!"
# 2.x versions are in the beta channel and prints "Hello snap guru!"
BASEDIR=`dirname "$0"`
grep -q "version: 2." $BASEDIR/meta/snap.yaml
if [ $? -eq 0 ]; then
  echo "Hello snap guru!"
else
  echo "Hello world!"

fi
